Write a compact Python function that merges two sorted lists without using sorted().
Implement a TypeScript LRU cache with get and put in O(1) time.
Write a C++ function that returns the length of the longest substring without repeating characters.
Create a Bash script fragment that retries a command up to three times with exponential backoff.
